import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Mathsy Admin - Teacher Assignment Dashboard',
  description: 'Manage teacher assignments, student inquiries, and demo scheduling with ease.',
  keywords: 'teacher management, student assignment, demo scheduling, tuition management',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
